{
  "name": "structured-world-model",
  "version": "1.0.0",
  "private": true,
  "description": "Unified semantic engine: extraction + compilation + governance",
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @swm/core test",
    "test:e2e": "pnpm --filter @swm/core test:e2e",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @swm/core dev"
  }
}
